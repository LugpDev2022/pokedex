import { isPokemonFavourite } from "../../../src/pokedex/helpers/isPokemonFavourite";

jest.mock("firebase/firestore/lite");

describe("tests on isPokemonFavourite", () => {
  beforeEach(() => jest.clearAllMocks());

  test("should", () => {
    getDoc.mockReturnValue();
  });
});
